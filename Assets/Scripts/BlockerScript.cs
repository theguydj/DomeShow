using UnityEngine;

public class BlockerScript : MonoBehaviour
{
    public BlockerScript NextBlockerLeft;
    public BlockerScript NextBlockerRight;
    public BlockerScript LeftBlocker;
    public BlockerScript RightBlocker;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
